<!DOCTYPE html>
<html lang="en">
<%= render :partial => "header" %>
<body data-spy="scroll" data-offset="64" data-target="#section-topbar">
<div class="row">
    <div class="col-12">
        <div class="text-center my-5">
            <h4 class="font-weight-bold">レッスンを追加してください</h4>
        </div>
        <%= form_for @yoga, url:create_confirm_yogas_path do |f|  %>
            <div class="form-group mx-5 pb-2 text-left">
                <%= f.label :date, "Date", class:"pr-2 font-weight-bold new__label" %>
                <div class="input-group date" id="datepicker" data-target-input="nearest">
                    <%= f.text_field :date, class: "form-control datepicker-input", data: {target: '#datepicker'} %>
                    <div class="input-group-append" data-target="#datepicker" data-toggle="datepicker">
                        <div class="input-group-text"><i class="fa fa-calendar"></i></div>
                    </div>
                </div>
            </div>
            <div class="form-group mx-5 pb-2 text-left">
                <%= f.label :lesson_id, "Lesson", class:"pr-2 font-weight-bold new__label" %>
                <%= f.collection_select :name, Lesson.all, :name, :name, {prompt: ""}, {class: "form-control text-monospace"} %>
            </div>
            <div class="text-center">
                <%= f.submit :class=>"btn-center btn-outline-warning btn-lg" %>
            </div>
        <% end %>
    </div>
</div>
<script type="text/javascript">
    $(function () {
        $('#datepicker').datetimepicker();
    });
</script>
</body>
</html>